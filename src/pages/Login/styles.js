/**
 * pages/Login/styles.js
 */
import { makeStyles } from '@material-ui/core/styles';
const config = {
  main: `
    position: relative;
  `,
  formRoot: `
    margin-top: 20px;
  `,
};

export default makeStyles(config, { name: 'LoginPage' });

/**
 * styles/general.js
 */
import { makeStyles } from '@material-ui/core/styles';
export const config = {
  root: `
    position: relative;
  `,
};

export default makeStyles(config, { name: 'LibGeneral' });
